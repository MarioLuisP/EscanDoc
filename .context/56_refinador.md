
● El árbol está completo ahora. Diagrama final real:

TFLite → foto                → intocable
TFLite → recibo              → factura (keywords + >80 bloques)
→ recibo
TFLite → folleto             → factura (keywords + >80 bloques)
→ recibo (aspectRatio > 2.0)
→ folleto
TFLite → documento/manuscrito → manuscrito (real: pocos bloques + pocos chars)
→ recibo (aspectRatio > 2.0)
→ factura (keywords + >80 bloques)
→ documento
